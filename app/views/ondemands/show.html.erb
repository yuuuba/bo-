<main>
  <ondemand-title>
    <%= @ondemand.title %>
  </ondemand-title>

  <div class="ondemand-content">
    <%# img %>
    <%= @ondemand.body %>
    <%# prof-img %>
    <%# 問い合わせるbtn %>
    <% if user_signed_in? && current_user.id == @ondemand.user_id %>
      <%= link_to "編集", action: "edit" %>
    <% end %>
  </div>
</main>